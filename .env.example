# Polymarket Scalp Radar - Configuration
# Copy this file to .env and fill in your credentials:
#   cp .env.example .env

# ── CREDENTIALS ──────────────────────────────────────────────
# Private key exported from Polymarket wallet (0x...)
POLYMARKET_API_KEY=0xYOUR_PRIVATE_KEY_HERE

# ── TRADING ──────────────────────────────────────────────────
# Maximum exposure limit in USD (open positions + pending orders)
POSITION_LIMIT=76

# USD amount per trade (can also be overridden via command line: python radar_poly.py 10)
TRADE_AMOUNT=4

# Price threshold that triggers audio alert (beep) on the dashboard
PRICE_ALERT=0.80

# Minimum signal strength (0-100) to trigger opportunity beep
SIGNAL_STRENGTH_BEEP=50

# ── INDICATOR PERIODS ────────────────────────────────────────
# RSI period (lower = more reactive, default 7)
RSI_PERIOD=7

# MACD periods (fast/slow/signal — lower = faster signals)
MACD_FAST=5
MACD_SLOW=10
MACD_SIGNAL=4

# Bollinger Bands period and standard deviations
BB_PERIOD=14
BB_STD=2

# ADX period (trend strength measurement)
ADX_PERIOD=7

# ── SIGNAL WEIGHTS ───────────────────────────────────────────
# Component weights for signal score (must sum to ~1.0)
# BTC Momentum (RSI + candle score)
W_MOMENTUM=0.30
# Divergence (BTC price vs Polymarket price)
W_DIVERGENCE=0.20
# Support/Resistance levels
W_SUPPORT_RESISTANCE=0.10
# MACD histogram delta (momentum acceleration)
W_MACD=0.15
# VWAP position + slope
W_VWAP=0.15
# Bollinger Bands position
W_BOLLINGER=0.10

# ── VOLATILITY ───────────────────────────────────────────────
# ATR/price ratio threshold to flag high volatility (default 0.03 = 3%)
VOL_THRESHOLD=0.03
# Score multiplier when high volatility detected
VOL_AMPLIFIER=1.3

# ── REGIME ADJUSTMENT ────────────────────────────────────────
# CHOP regime: dampen signal (0.5 = reduce by 50%)
REGIME_CHOP_MULT=0.50
# TREND aligned: boost signal (1.15 = +15%)
REGIME_TREND_BOOST=1.15
# Counter-trend: reduce signal (0.7 = -30%)
REGIME_COUNTER_MULT=0.70

# ── PHASE THRESHOLDS ────────────────────────────────────────
# Min signal strength per market phase (time remaining in 15-min window)
# EARLY (>10 min left): conservative
PHASE_EARLY_THRESHOLD=50
# MID (5-10 min left): normal
PHASE_MID_THRESHOLD=30
# LATE (1-5 min left): very selective
PHASE_LATE_THRESHOLD=70
